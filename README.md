# Weather-App
A HTML, Java and CSS based program that fetches data from a local JSON file and displays it on a program. A user can enter a city that is in the JSON file and the cities temperature, humidity, UvIndex and wind speed is displayed on separate web pages.
